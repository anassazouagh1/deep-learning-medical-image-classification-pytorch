[project]
name = "raravis-dl-image-classification"
version = "1.0.0"
description = "PyTorch pipeline for multi-class medical image classification"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
